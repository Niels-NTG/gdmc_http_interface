{
	"$schema": "http://json-schema.org/draft-06/schema#",
	"type": "array",
	"items": {
		"$ref": "#/definitions/BlockPlacementResult"
	},
	"definitions": {
		"BlockPlacementResult": {
			"type": "object",
			"additionalProperties": false,
			"properties": {
				"status": {
					"type": "integer",
					"title": "Placement result status",
					"description": "1 if placement has been successful, 0 if block at placement position is identical or if some error has occurred. In the latter case, an error message will be included."
				},
				"message": {
					"type": "string"
				}
			},
			"required": [
				"status"
			],
			"title": "Block placement result"
		}
	}
}
