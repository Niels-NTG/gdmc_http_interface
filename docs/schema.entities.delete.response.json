{
	"$schema": "http://json-schema.org/draft-06/schema#",
	"type": "array",
	"items": {
		"$ref": "#/definitions/EntityDeleteResult"
	},
	"definitions": {
		"EntityDeleteResult": {
			"type": "object",
			"additionalProperties": false,
			"properties": {
				"status": {
					"type": "integer",
					"description": "1 if removing the entity was successful, 0 if no entity with the given UUID was found or the UUID was invalid."
				},
				"message": {
					"type": "string",
					"description": "An error message if removing the entity failed."
				}
			},
			"required": [
				"status"
			],
			"title": "Entity delete result"
		}
	}
}
